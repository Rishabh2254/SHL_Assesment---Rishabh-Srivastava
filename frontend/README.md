# SHL Assessment Recommendation System - Frontend

## Setup

1. Install dependencies:
```bash
npm install
```

2. Set the API URL (optional, defaults to http://localhost:8000):
```bash
# Create .env.local file
NEXT_PUBLIC_API_URL=http://localhost:8000
```

3. Run the development server:
```bash
npm run dev
```

4. Open [http://localhost:3000](http://localhost:3000) in your browser

## Build for Production

```bash
npm run build
npm start
```

## Features

- Modern, responsive UI with TailwindCSS
- Dark mode toggle
- Loading animations
- Error handling
- Recommendation table with clickable URLs
- Framer Motion animations

## Deployment

See `docs/submission_instructions.md` for deployment instructions on Vercel.

